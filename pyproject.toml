[project]
name = "store-path-optimizer"
version = "0.1.0"
description = "Streamlit app for optimizing in-store walking paths"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "streamlit>=1.32",
    "matplotlib>=3.8"
]